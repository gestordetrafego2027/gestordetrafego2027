import {pageMetadata} from '@/lib/seo/metadata'

export async function generateMetadata({params}) {
  const {locale} = await params
  return pageMetadata({
    path: '/portfolio-produtora/barbara-porto',
    locale,
    title: 'Barbara Porto | Produtora — House Mazzutti',
    description: 'Produção fotográfica Barbara Porto — direção de imagem e fotografia de produto pela House Mazzutti. Veja o ensaio completo.',
  })
}

export default function Layout({children}) {
  return children
}
